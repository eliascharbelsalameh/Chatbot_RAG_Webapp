# --- Core RAG ---
langchain>=0.3.9
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-core>=0.3.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0  # also provides CrossEncoder for the BGE reranker
transformers>=4.44.0
torch>=2.2.0

# --- LLM (served by LM Studio's OpenAI-compatible local server) ---
langchain-openai>=0.2.0
openai>=1.50.0
# llama-cpp-python is no longer used at runtime; left here for the legacy main.py:
# llama-cpp-python>=0.3.0

# --- Crawling ---
# curl_cffi: Chrome TLS fingerprint impersonation. Needed because stellantis.com
# is fronted by a WAF that 403s any client with a non-browser TLS handshake.
curl_cffi>=0.7.0
scrapy>=2.11.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Document ingestion ---
pdfplumber>=0.11.0
python-docx>=1.1.0
pandas>=2.0.0
openpyxl>=3.1.0

# --- UI ---
streamlit>=1.36.0
streamlit-audiorec>=0.1.3

# --- Audio (local, free) ---
faster-whisper>=1.0.0

# --- Misc ---
python-dotenv>=1.0.0
pyperclip>=1.8.2
pyttsx3>=2.90
