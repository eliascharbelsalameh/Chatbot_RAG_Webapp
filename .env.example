# Copy to .env and adjust. Anything left unset falls back to defaults in config.py.

# --- LLM (local GGUF via llama-cpp-python) ---
# RAG_LLM_PATH=C:\Users\elias\.cache\lm-studio\models\lmstudio-community\Meta-Llama-3.1-8B-Instruct-GGUF\Meta-Llama-3.1-8B-Instruct-Q4_K_M.gguf
# RAG_LLM_N_CTX=8192
# RAG_LLM_N_GPU_LAYERS=-1
# RAG_LLM_TEMPERATURE=0.0

# --- Embeddings ---
# RAG_EMBED_MODEL=BAAI/bge-m3
# RAG_EMBED_DEVICE=cuda

# --- Reranker ---
# RAG_USE_RERANKER=true
# RAG_RERANK_MODEL=BAAI/bge-reranker-v2-m3

# --- Crawl ---
# RAG_CRAWL_START_URLS=https://www.stellantis.com/en
# RAG_CRAWL_ALLOWED_DOMAINS=stellantis.com
# RAG_CRAWL_MAX_PAGES=2000
# RAG_CRAWL_DEPTH_LIMIT=0
# RAG_CRAWL_DOWNLOAD_DELAY=0.5
